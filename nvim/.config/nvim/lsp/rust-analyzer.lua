return {
  cmd = { "rust-analyzer" },
  filetypes = { "rust" },
  settings = {
    ["rust-analyzer"] = {},
    checkOnSave = {
      enable = false
    },
    diagnostics = {
      enable = false
    }
  }
}
